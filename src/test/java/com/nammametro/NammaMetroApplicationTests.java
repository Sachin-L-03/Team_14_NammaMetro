package com.nammametro;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test — verifies that the Spring application context loads successfully.
 */
@SpringBootTest
class NammaMetroApplicationTests {

    @Test
    void contextLoads() {
        // If this test passes, the Spring context (all beans, configs, entities)
        // has been wired correctly.
    }
}
