package com.nammametro.model.enums;

/**
 * Possible statuses for a schedule entry.
 *
 * SRP: This enum has one responsibility — defining the set of valid schedule statuses.
 */
public enum ScheduleStatus {
    ACTIVE,
    CANCELLED,
    COMPLETED
}
