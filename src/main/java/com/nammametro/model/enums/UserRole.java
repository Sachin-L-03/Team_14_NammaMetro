package com.nammametro.model.enums;

/**
 * Roles available to users in the Namma Metro system.
 *
 * SRP: This enum has one responsibility — defining the set of valid user roles.
 */
public enum UserRole {
    PASSENGER,
    OPERATOR,
    ADMIN
}
