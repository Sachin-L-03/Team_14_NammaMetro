package com.nammametro.model.enums;

/**
 * Severity levels for incidents.
 *
 * SRP: This enum has one responsibility — defining the set of valid severity levels.
 */
public enum Severity {
    LOW,
    MEDIUM,
    HIGH
}
